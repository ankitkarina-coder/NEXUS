# Authentication boundary

Authentication providers, session/token validation, and identity context belong here. Route handlers should consume authenticated context rather than implement authentication themselves.