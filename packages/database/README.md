# Database boundary

Place the database schema, migrations, client, and repositories in this package. API modules should depend on repository interfaces or services, never on a database client directly.